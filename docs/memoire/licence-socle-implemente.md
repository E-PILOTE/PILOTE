---
name: licence-socle-implemente
description: Socle CLIENT du système de licence offline-first implémenté & vérifié (lib/licensing/) + audit C1-C6 + ADR
metadata: 
  node_type: memory
  type: project
  originSessionId: ffc12413-1442-4bf8-8470-d36a30e6cfe1
---

Implémentation du socle client du système de licence (2026-07-04), après audit implémenteur ruthless. Complète [[abonnement-architecture-offline]] / [[abonnement-technique-powersync]] / [[abonnement-etat-reel-enforcement]].

**Décision d'audit : 🟡 approuvé sous corrections C1–C6** (gelées, dans `docs/adr/ADR-licence.md`) :
- C1 supprime abstractions prématurées (VersionAnchor/IdentityContext/RefreshSignal) → 4 ports seuls ; repères fondus dans `TrustState` ; `license_pointer` différée.
- C2 pas d'`EntitlementService` (état=provider Riverpod, logique=objet `Entitlement`).
- C3 (défaut critique) anti-rollback = **`version` monotone SEULE** ; `issued_at` ne rejette JAMAIS (sinon lockout pile-CMOS-morte).
- C4 (invariant) la licence **ne gate JAMAIS la synchro PowerSync** — UI/features uniquement.
- C5 online (`subscription_access`) et offline (licence) = 2 politiques, non fusionnées.
- C6 module transverse `lib/licensing/` (pas `features/`).

**Livré & VÉRIFIÉ** (`epilote/lib/licensing/`, îlot hexagonal) : domain (License, Entitlement, computeLicensePhase double-horloge, LicenseValidator, ports+TrustState) · application (LicenseService : bootstrap/refresh/swap atomique/fail-soft/bestKnownNow) · infrastructure (Ed25519Verifier via pkg `cryptography`, SecureLicenseStore via `flutter_secure_storage`, MonotonicClock, SupabaseLicenseGateway→Edge Function `license-issuer`) · presentation (`license_providers.dart`). **Verrou plan** inséré dans `app_router.dart` `redirect` avant le verrou 3 (fail-soft).

**Vérification** : `flutter analyze` = 0 ; **23 tests verts** (`test/licensing/`) dont **round-trip Ed25519 réel** (génère paire→signe JWS→vérifie→altère→kid inconnu) ; **`flutter build linux --debug` OK**. ⚠️ build Linux exige `libsecret-1-dev` (installé). Deps ajoutées : `cryptography ^2.7.0`, `flutter_secure_storage ^9.2.4`.

**Enforcement DORMANT** : `licensePinnedKeysProvider` = `{}` (vide) → aucun token ne décode → `Entitlement.none()` → comportement app strictement préservé. **Activer** = générer paire Ed25519 + déployer Edge Function + renseigner clés publiques épinglées.

**Landé sur main** : PR #3 (socle + ➊➋ online), PR #4 (Edge Function draft + refresh client dormant + `docs/LICENCE_GOLIVE.md`).

**⚡ ACTIVÉ EN PROD (pilote METP) le 2026-07-04 — VÉRIFIÉ E2E (GUI)** (PR #5, non mergée) :
- mig **0026 appliquée** (`school_groups.license_version` + `next_license_version()` = compteur monotone).
- Edge Function **`license-issuer` DÉPLOYÉE** (CLI binaire v2.30.4 récupéré via GitHub + `SUPABASE_ACCESS_TOKEN` fourni par l'user ; le wrapper npm est cassé sur linux-x64). Secrets posés : `LICENSE_PRIVATE_KEY_PKCS8_B64`, `LICENSE_KID=2026-07`, `LICENSE_OFFLINE_WINDOW_DAYS=30`, **`LICENSE_PILOT_GROUP_IDS=da3954ca…` (METP seul)**. Paire Ed25519 : privée = secret fonction (jamais imprimée) ; publique **épinglée** dans `licensePinnedKeysProvider` (kid 2026-07).
- **Preuve e2e** : login `directeur@kinkala.cg` (METP) → fonction émet un JWS (signature Ed25519 **VALIDE** contre la clé épinglée, 28 modules, `license_version` → 2) → coffre → phase **grâce** (METP échu 2026-06-28, J+6) → **bannière `LicenseBanner` « Abonnement échu » affichée** au dashboard, **synchro toujours active (C4)**, aucun lockout. `flutter analyze` 0 · 23 tests · build Linux OK.
- **Garde-pilote** : groupes hors METP → 403 → dormants. **Rollback** = vider `licensePinnedKeysProvider` OU `LICENSE_PILOT_GROUP_IDS=`.

**Vague 6 FAITE** (write-gating staff) : verrou licence greffé sur `canProvider` (chokepoint, 81 usages) → actions mutantes coupées en lecture seule, `read`/`export` toujours permis (N12), fail-soft. 3 tests (`can_provider_license_test.dart`). 26 tests Flutter verts.

**C4 FAIT** (dernier 🔴, licence 2 étages) : mig 0027 `payment_confirmed` (défaut true) ; issuer plafonne `valid_to`=MIN(fin, now+`LICENSE_PROVISIONAL_DAYS`=7) si paiement non confirmé ; logique extraite `_valid_to.ts` (deployé==testé) + **5 tests deno**. Redéployée. E2e non-régression METP (payé→plein terme, provisional:false, version→3). Chemin provisoire non exercé en prod (garde-fou anti-mutation tenant) → couvert par tests deno.

**G3 FAIT** (fenêtre adaptative) : mig 0028 `offline_window_days` (override nullable/zone, NULL=30j généreux) ; issuer : paiement non confirmé → fenêtre provisoire courte (prime, couplé C4), sinon override zone, sinon défaut. 4 tests deno (9 total). Redéployée, e2e non-régression METP (30j).

**Colonnes issuer (school_groups)** : `license_version` (mig 0026, compteur), `payment_confirmed` (0027, défaut true), `offline_window_days` (0028, nullable). Secrets fonction : `LICENSE_PRIVATE_KEY_PKCS8_B64`, `LICENSE_KID=2026-07`, `LICENSE_OFFLINE_WINDOW_DAYS=30`, `LICENSE_PILOT_GROUP_IDS`, (`LICENSE_PROVISIONAL_DAYS` défaut 7).

**Uniformité plateforme VÉRIFIÉE + FERMÉE (ADR-0008)** : le write-gating staff avait une particularité — seuls les écrans via `canProvider` (verrou 3, masque boutons) étaient couverts. Ajout du **vrai chokepoint d'écriture** `runModuleWrite` (52 sites écrans) : refuse l'écriture LOCALE en lecture seule via `LicenseEnforcement.writeBlockedNow` (miroir global static de l'`Entitlement` courant, alimenté par `EntitlementNotifier.build`/`refreshLicense`, lisible sans `ref`). **Couvre les 7 domaines catalogue** (classes/evaluation/finance/staff/structure/students/vie_scolaire). **Exclusions ASSUMÉES** (documentées ADR-0008, PAS un oubli) : `features/communication/` (tissu natif hors catalogue) + `features/user/` (espace perso) écrivent hors chokepoint — la licence gate les MODULES facturables, jamais le canal de communication ni le self-service (esprit N12). admin_groupe : scope intentionnel « pas de croissance » (bloque création écoles/users, gestion+exports OK) ; messages bandeau/snackbar alignés. 3 tests `LicenseEnforcement` → **40 tests verts**, analyze 0. Commits `e51d12c`+ADR sur PR #5.

**⚡ FLEET-WIDE ACTIVÉ 2026-07-05 (PR #7)** : enforcement élargi du pilote METP à TOUTE la plateforme. Rayon de souffle mesuré AVANT (psql pooler `aws-1-eu-central-2`, mdp postgres) = 7 groupes seulement (plateforme jeune) : 6 actifs (modules 7→28, phase active) + 1 échu METP (grâce) ; **0 lecture seule, 0 plan_id NULL, 0 plan à modules vides** → flip sûr. Bascule = `supabase secrets unset LICENSE_PILOT_GROUP_IDS` (issuer : variable absente = fleet-wide ; définie même vide = allowlist). **Garde fail-soft ajouté** dans issuer : groupe sans plan OU 0 module actif → `403 not_provisioned` → reste dormant/permissif (jamais brické par licence modules-vides) → protège l'auto-enrôlement des futurs groupes. Fonction redéployée (CLI binaire v2.30.4 scratchpad + PAT). **Kill-switch instantané** = re-set `LICENSE_PILOT_GROUP_IDS=` (vide). e2e second-groupe NON fait (pas d'identifiants staff) → couvert par vérif base + revue code. Doc `LICENCE_GOLIVE.md §5` actée. **E2E GUI VÉRIFIÉ (2026-07-05)** sur vrai appareil staff METP (Collège Public de Kinkala, agent Aline Massamba PIN via écran-verrou poste-partagé) : bannière licence **grâce ambre** affichée (« Abonnement de l'établissement échu — régularisation requise… », rendu exact `license_banner.dart`), **synchro active** (C4 OK), dashboard complet (grâce = accès + alerte), 0 exception runtime, refactor tick `ConsumerStatefulWidget` rend OK. ⚠️ METP était déjà pilote → prouve pipeline+code+garde-n'a-pas-cassé-METP, PAS le delta d'expansion pour un NOUVEAU groupe (exigerait un appareil staff non-METP). GUI-testing : `flutter run -d linux` fenêtre `epilote`, `import -window`+`xdotool mousemove --window`, coords client = image full-res 2560x1368. **admin_groupe (online) AUSSI vérifié GUI** (vianney M., METP) : `SubscriptionBanner` grâce ambre « Abonnement expiré depuis 7 jours — renouvelez pour éviter le passage en lecture seule. » + bouton Renouveler + badge « Expiré » = `_graceMessage(7)` exact. Dashboard complet (9 écoles/297 élèves/84 personnel). **Les DEUX horloges s'accordent** : staff offline ET admin online = METP en grâce. Refactor tick `ConsumerStatefulWidget` rend OK des deux côtés. NB : classifieur auto-mode bloque le clic de déconnexion sur kiosque école live (état partagé prod) → user a fait le logout/login admin manuellement. Restaurer Kinkala = login `directeur@kinkala.cg`. Comptes test : vianney@epilote.cg (admin_groupe METP) / directeur@kinkala.cg (directeur Kinkala) / PIN agent Aline Massamba=1994, tous psw `‹secret — gestionnaire de mots de passe›`.

**Vague 3 COMPLÈTE (tick ré-évaluation, PR #6)** : dernier item ouvert V3 = « tick 1×/jour + premier plan ». La phase dérive de `now` mais l'`Entitlement`/`SubscriptionAccess` en mémoire ne bouge pas sur session longue HORS LIGNE → bascule grâce→lecture-seule invisible sans redémarrage. Fix : `LicenseBanner` (staff) + `SubscriptionBanner` (admin) → `ConsumerStatefulWidget`+`WidgetsBindingObserver`, `setState`/`invalidate` au `resumed` + `Timer.periodic(6h)`. NB : `writeBlockedNow` lisait déjà `now()` → enforcement déjà correct ; le tick ne ferme que le décalage de VISIBILITÉ. **Docs réconciliées** : §11 `ABONNEMENT_LICENCE_ARCHITECTURE` (V0→V3 cochées, correction `subscriptions`→`school_groups`) + « Reste à faire » `ABONNEMENT_ARCHITECTURE_LOGICIELLE`. **Programme §11 : V0/1/2/3 ✅, V4 différée.** analyze 0, 40 tests.

**Stress-test : 3/5 trous fermés** (C4✅ F2/F3✅-kid G3✅) ; **RESTE** : N8 (activation offline USB/QR) + E1/E2 (fusion/scission tenant) = **délibérément NON construits (prématuré, à faire au vrai besoin)** ; quotas souples = advisory jamais-bloquant (risque déjà évité, `quotas:{}`) documenté backlog ; Vague 8 `license_pointer` ; **merger PR #5** (self-merge bloqué → geste user) + élargir `LICENSE_PILOT_GROUP_IDS`. Tout sur `feat/licence-activation-prep` (PR #5).
