---
name: modules-natifs-communication
description: Communication = tissu natif (hors catalogue/plans) ; catalogue réduit à 36 modules / 7 catégories
metadata: 
  node_type: memory
  type: project
  originSessionId: fbfd7c02-473f-415b-b6e1-a21454a8de92
---

DÉCISION ARCHITECTURE (2026-06-06). La communication & l'engagement sont désormais du **tissu natif de la plateforme** (comme Utilisateurs / Profils d'accès) : présents par boutons fixes dans la sidebar, **jamais vendus ni attribués**, non désactivables. Ils ont été **retirés du catalogue** `modules`/`module_categories`.

**Supprimés en base** (migration définitive) : 5 modules `notifications, annonces, messagerie, evenements, espace-parent` + leur catégorie `communication` (devenue vide). Cascade : 178 `profile_permissions` + 17 `plan_modules` supprimés d'abord (FK). `subscription_plans.module_count` recalculé.

**Nouvel état catalogue** : **7 catégories, 36 modules**. Plans : Gratuit 7, Premium 20, Pro 32, Institutionnel 36. Le catalogue = **uniquement le métier**.

**Code** : boutons natifs section « COMMUNICATION » ajoutés dans `app_shell.dart` pour admin_groupe (Notifications/Annonces/Messagerie/Événements) et personnel (idem + règles rôle). super_admin avait déjà sa suite. Routes + placeholders dans `routes.dart` / `app_router.dart` (`adminAnnonces/adminMessagerie/adminNotifications/adminEvenements` ; `notifications/messagerie/evenements/espaceParent` côté user). **Écrans à construire** (placeholders pour l'instant). Presets `admin_access_screen.dart` nettoyés de toute clé `'communication'`.

**Règles métier (garde-fous)** :
- **Élèves exclus de la messagerie privée** (sauvegarde mineurs) → pas de bouton Messagerie pour `role=eleve`.
- **Espace Parent** = bouton réservé `role=parent` ; natif mais **désactivable par école** via `schools.parent_portal_enabled boolean DEFAULT true` (colonne ajoutée ; **lecture du flag pas encore câblée dans la sidebar — TODO**).

**FACTORISATION (2026-06-06)** : module partagé `features/communication/` scope-aware créé. `providers/communication_scope.dart` = `CommScope {platform, group, school}` + `communicationContextProvider` (déduit du rôle). **Notifications = patron de référence FAIT** : découpé en fichiers ≤ 285 lignes (providers/notifications_provider [scope-aware], widgets/notification_types|timeline|send_notification_dialog|notification_bell|notifications_drawer). super_admin migré + admin_groupe branché. **Annonces = FAIT** : 1942 lignes → 6 fichiers `part`/`part of` ≤457 + provider scope-aware. **Messagerie = FAIT (2026-06-07)** : ancien `super_admin/screens/messages_screen.dart` (1148) + `super_admin/providers/messages_provider.dart` SUPPRIMÉS → `communication/screens/messages_screen.dart` (lib 80) + parts `messages_sidebar|list|reader|compose|states` (≤383) + `providers/messages_provider.dart` (224) scope-aware. Compose scope-aware : plateforme→choisir un GROUPE (recipient=admin du groupe via `groupAdminRecipient`) · groupe→choisir un UTILISATEUR du groupe (RPC `get_group_users`). Noms résolus via embed `sender:profiles!sender_id(...)`/`recipient:profiles!recipient_id(...)` + `MessageModel.counterpart(myId)` (libellé interlocuteur). Routes `superMessagesInbox` + `adminMessagerie` branchées dessus. **Événements = FAIT (2026-06-07, net-new)** : table `events` (group_id NOT NULL, school_id, title, event_date, end/start/end_time, location, target_audience, is_published, created_by). `providers/events_provider.dart` (188) scope-aware + `screens/events_screen.dart` (404) [KPI/filtres À venir·Tous·Publiés·Brouillons·Passés/cartes date+menu publier/éditer/supprimer] + part `event_form.dart` (338, date/time pickers, audience, switch publier). Route `adminEvenements` branchée. École/offline (db.watch) = lot ultérieur (routes user `annonces/messagerie/evenements/notifications` encore placeholders).

**COMPLÉTUDE FONCTIONNELLE (2026-06-07)** :
- **Realtime** ajouté sur Messagerie, Notifications, Événements + badge messagerie sidebar (`unreadMessagesCountProvider`) — pattern debounce 1s + `channel().onPostgresChanges(all)` + `ref.invalidateSelf()` + `onDispose(removeChannel)`, copié d'`announcements_provider`. Avant : seul Annonces avait le realtime.
- **Messagerie = fils de conversation** : `threadOf(all, m)` (remonte `parent_message_id` → racine, collecte le fil trié) + `baseSubject()` (retire les « RE: ») dans `messages_provider.dart` ; reader rendu en **bulles** (`_Bubble`, moi=droite navy / autre=gauche) + méta « N messages ».
- **Événements = vue Calendrier** : toggle Liste/Calendrier (`eventViewProvider`) ; `event_calendar.dart` (part) = grille mensuelle custom (pas de dépendance ajoutée) avec pastilles publié(vert)/brouillon(ambre), nav mois, « Aujourd'hui », drill-down jour → `_EventCard`.
- **ÉMETTEURS DE NOTIFICATIONS** : `database/migrations/0001_notification_emitters.sql` — helper `emit_notification()` + `primary_tutor_user()` + 4 triggers SECURITY DEFINER : facture créée→admin_groupe (route `/admin/abonnement`), paiement élève→tuteur, absence/retard→tuteur, bulletin publié→tuteur. Schéma vérifié LIVE via OpenAPI REST (psql/MCP indispo). ⚠️ **PAS encore appliqué** — à exécuter dans Supabase SQL Editor (comme le redéploiement sync-rules). Sans ça le centre de notifications reste vide. FCM push = TODO séparé. Enums live : attendance_entries.status{present,absent,late} · bulletins.status{draft,submitted,validated,published} · student_payments.status{pending,confirmed,cancelled,refunded} · group_invoices.status{pending,paid,overdue,cancelled}.

**MESSAGERIE — actions complétées (2026-06-07)** : ajout `markMessageUnread` + `deleteMessage` (provider) ; reader header = Répondre · **Transférer** (compose pré-rempli « TR: » via `_ComposeDialog(initialSubject/initialBody)`) · **Marquer non lu** (si lu) · Archiver/Désarchiver · **Supprimer** (confirm) · Fermer. messages_reader.dart = 482 lignes.

**ESPACE SUPPORT admin_groupe (2026-06-07)** : asymétrie corrigée — super_admin avait l'espace Tickets (reçoit/répond/change statut via `tickets_provider`+`tickets_screen` 746L), mais l'admin_groupe ne pouvait soumettre que via une carte enterrée dans Paramètres. Nouveau : `providers/admin_support_provider.dart` (AdminTicket, `adminTicketsProvider` scope groupe + **realtime** sur `support_tickets`, `createSupportTicket`, KPIs, `adminTicketStatusFilter`) + `screens/admin_support_screen.dart` (419L : KPI total/ouverts/en cours/traités, filtres, cartes ticket avec badges statut/priorité + **réponse du support** affichée, dialog création objet/catégorie/priorité/description). Route `Routes.adminSupport='/admin/support'` + entrée sidebar « Support » (section SYSTÈME). Table `support_tickets` (LIVE) : group_id, submitted_by, subject, body, category, status{open,in_progress,resolved,closed}, priority{low,medium,high,urgent}, assigned_to, resolved_at, **response** (réponse unique, pas un fil), attachment_url. Boucle complète : admin crée → super voit/répond → admin voit la réponse en realtime. ⚠️ La carte « Mes demandes au support » dans Paramètres existe encore (redondance mineure tolérée ; le flux `requestGroupUpdate` y reste pour la modif de groupe).

**HEADER NOTIFICATIONS (2026-06-07)** : l'icône cloche du top header (super_admin + admin_groupe) est maintenant un **dropdown** (`widgets/notification_bell.dart`, `MenuAnchor`) = aperçu 5 dernières notifs (deep-link au clic via `data.route`, « Tout lire ») + bouton « Tout afficher » qui ouvre le **drawer** complet (`endDrawer` du Scaffold AppShell). **L'icône Messagerie a été RETIRÉE du top header** (demande user) ; le compteur non-lus (`unreadMessagesCountProvider`) est **déplacé en badge sur l'entrée sidebar « Messagerie »** (admin_groupe + personnel). `_HeaderBadgeButton` supprimé d'app_shell.

**MESSAGERIE — pièces jointes / audio / emoji / multi-destinataires (2026-06-07)** : migration `database/migrations/0002_message_attachments.sql` **APPLIQUÉE EN PROD** (via pooler Supavisor `aws-1-eu-central-2:5432`, user `postgres.<ref>`, pwd postgres — ⚠️ le pooler est `aws-1-` pas `aws-0-`, et seuls 443/6543/5432 vers le pooler sont ouverts ; `db.<ref>.supabase.co:5432` direct est BLOQUÉ). Contenu : (1) colonne `messages.attachments jsonb NOT NULL DEFAULT '[]'` = `[{url,name,mime,size,kind}]` ; (2) **BUG RLS corrigé** — `messages` avait RLS activée mais SEULEMENT `msg_insert`+`msg_select` → `markRead/markUnread/archive/delete` n'affectaient 0 ligne pour admin_groupe (silencieux). Ajout `msg_update`+`msg_delete` (super_admin OR membre groupe sender/recipient) ; (3) bucket Storage **public** `message-attachments` (chemins `{groupId}/{uuid}_{nom}` imprévisibles ; folder='platform' pour super_admin) + policies insert/update/delete authenticated. **Flutter** (0 lint, build ✓) : `MessageAttachment` (+ `mimeForExtension`/`kindFor`) + `uploadMessageAttachment()` + `sendMessageToMany()` (fan-out 1 ligne/destinataire) dans messages_provider (433L). Nouveau part `messages_attachments.dart` (335L) = `_AttachmentView` (lecteur : image=thumbnail CachedNetworkImage, audio/fichier=chip → `launchUrl` externe), `_AttachmentEditList` (composer), `_EmojiPicker` (grille 60 emojis curatée, **sans dépendance**, insert au curseur), `_RecipientMultiSelect` (recherche + cases à cocher). `messages_compose.dart` (372L) réécrit : multi-select destinataires + barre d'outils (fichier `FileType.any`/image/audio via `file_picker` `withData:true` + emoji toggle) + fan-out à l'envoi (groupe→`sendMessageToMany` ; plateforme→1 `sendMessage` par groupe avec résolution admin). **Aucune nouvelle dépendance** (file_picker/url_launcher/cached_network_image/uuid déjà présents). Pas d'enregistrement audio in-app (risque natif Linux) → audio = pièce jointe audio + écoute externe. Reply inline (`_ReplyForm`) reste texte-seul (quick-reply ; le compose complet a tout).

**SUPPORT admin_groupe — recherche + tri (2026-06-07)** : `admin_support_screen.dart` — barre de contrôle réorganisée : Row `[_SearchField (Expanded) · _SortMenu · « Nouvelle demande »]` puis Wrap des filtres statut en dessous. `adminTicketSearchProvider` (filtre objet+description) + `_SearchField` (clear). Tri déjà via `adminTicketSortProvider`/`sortTickets` (recent/old/priority/status).

Lié : [[modules-acces-hierarchie]] (le verrou 4 niveaux ne s'applique plus à ces 5 ex-modules), [[admin-groupe-espace]], [[regle-taille-fichier-500]]. analyze 0 issue, build ✓, plateforme relancée ✓.