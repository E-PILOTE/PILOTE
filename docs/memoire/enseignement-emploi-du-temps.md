---
name: enseignement-emploi-du-temps
description: "Page Emploi du temps refondue (3 vues, détection de conflits, PDF, saisie standard) — Enseignement entamé"
metadata: 
  node_type: memory
  type: project
  originSessionId: de141126-f9cd-431f-ab99-c4251de71ef4
---

✅ 2026-06-26 — **Emploi du temps** (`/user/emploi-du-temps`, slug `emploi-du-temps`) refondu aux standards SaaS scolaire (Pronote/Untis). **Non encore commité.**

**Ce qui a été ajouté** (au-dessus de la base sélecteur-classe→KPI→grille existante) :
- **3 vues** (enum `TtView` classe/enseignant/salle) via segmented toggle. Classe = édition ; Enseignant/Salle = lecture, clic créneau → bascule en vue classe sur sa classe. Sélecteurs : `ScopeDrilldownPanel` (classe) ou `_EntityPicker` dropdown (prof/salle, dérivés des créneaux existants).
- **Détection de conflits** (cœur correctness) : `detectConflicts()` O(n²) sur tous les slots école → familles `ConflictKind.teacher` / `room` / `classOverlap` (chevauchement même jour via `rangesOverlap`). `conflictingSlotIds()` → surlignage rouge + ⚠ dans la grille ; `_ConflictBanner` au-dessus de la grille ; dans le formulaire `conflictsForCandidate()` EN DIRECT → **prof/classe = blocage du bouton** (rouge « Conflit »), **salle = avertissement ambre** non bloquant.
- **Saisie rapide** : puces « séance standard » `kStdPeriods` (trame 55 min Congo, 9 séances) qui pré-remplit début/fin ; enseignant auto-rempli depuis `teacher_subjects` à la sélection de matière (`defaultTeacherForSubject`, écrasable, flag `_teacherTouched`).
- **Export PDF** A4 **paysage** : `EmploiDuTempsPdfService` (grille `pw.Table` Lun→Sam, chrome `OfficialPdfKit`, `showPdfPreviewDialog`, contenu de case adapté à la vue). `AdminPdfButton` en tête.

**Itération 2 (même jour, redesign après revue honnête)** : (1) **Vue d'ensemble = landing par défaut** (`TtView.ensemble`). ⚠️ NB : d'abord fait en cockpit-tableau chiffré → **rejeté par le user** (« il s'agit des emplois du temps, pas un tableau qui liste ») → refait en **MUR DE MINI-GRILLES** (`_OverviewGallery`) : une mini-grille hebdo par classe (jour×horaire, blocs colorés par matière avec abrégé 3 lettres p.ex. FRA/COM, conflits cerclés rouge), Wrap de cartes 300px groupées par cycle (`scopeCycleColor/Name/Order`), placeholder « Aucun créneau · cliquer pour bâtir » si vide, bandeau « N conflits établissement », clic carte → grille classe. (pas de % couverture → `classWeeklyTargetProvider` retiré) ; (2) **commutateur compact** `_ViewSwitch` (4 segments Ensemble/Classe/Enseignant/Salle) sur la ligne de titre, PDF/Ajouter contextuels à droite ; (3) **matière LIMITÉE au programme** de la classe dans le form (`classProgramProvider` ⋈ `class_subjects`+`teacher_subjects`, prof pré-rempli ; note si programme vide) ; (4) **multi-jours à la création** (chips Lun-Sam, crée N créneaux, jours en conflit ignorés avec résumé). **Vérifié en GUI réel** (Linux, `GDK_SCALE=1` pour le piège GPU 2×/HiDPI) : cockpit, switch, drill-through, 3 vues, conflits rouges cohérents, form (chips jours+séances, garde matière), **PDF paysage** rendu OK.

**Fichiers** (tous ≤500, part-files d'une lib) : `screens/emploi_du_temps_screen.dart` (orchestrateur ~432) + parts `_grid` / `_form` / `_widgets` / `_overview` (cockpit) ; `providers/timetable_provider.dart` (conflits+périodes+`classProgramProvider`+`classWeeklyTargetProvider`) ; `services/emploi_du_temps_pdf_service.dart` (paysage). Table `timetable_slots` inchangée. analyze 0 / build linux ✓.

⚠️ **Piège GUI test** : iMac = écran 2× Retina → `flutter run` crashe « Timed out waiting for OpenGL frame 2560x1368 (have 1280x720) → Lost connection ». Lancer avec `GDK_SCALE=1 GDK_DPI_SCALE=1` pour aligner le framebuffer. Fenêtre vivante = 2560×1368. Voir [[gui-testing-linux]].

**Itération 3 — REFONTE « page unique » sans onglets (2026-06-28, non commité, build+analyze OK)** : sur demande user « même style qu'Inscriptions, élimine les onglets, boutons/filtres/modals/drawers ». Le shell 3-onglets (Vue d'ensemble/Emplois du temps/Paramètres) a été SUPPRIMÉ → **une seule page scrollante** calquée sur Inscriptions :
- **Hero KPI globaux** (`_EdtKpiSection`, `AdminStatCard` `GridView` `mainAxisExtent:168`, cols 1180→6) : État (statut version), Conflits, Classes couvertes X/Y, Créneaux, Enseignants, Salles.
- **Panneau couverture** = `ScopeDrilldownPanel` partagé (`ok = la classe a des créneaux`, `metricLabel:'Avec EDT'`, `unitNoun:'classes'`). Cliquer une classe → bascule auto en vue Classe sur sa grille.
- **Barre de filtres** `_EdtFilterBar` (recherche matière/prof/classe/salle + `_ViewSegment` 4 vues + bouton icône **Paramètres** (tiroir) + icône **PDF** + bouton dégradé **Ajouter un créneau** (PermissionGate, visible vue Classe éditable)). Chip de scope actif + verrou « Année verrouillée ».
- **Commutateur de vue** `_ViewSegment` (Établissement/Classe/Enseignant/Salle) REMPLACE les onglets ET l'ancien `_AxisSwitch` (supprimé). Établissement → `_MasterCalendar` (tous cours, couleur par classe) ; Classe/Enseignant/Salle → titre + `_Kpis` entité + `_ConflictBanner` + `_TimetableGrid` (Classe éditable, autres lecture).
- **Paramètres = TIROIR latéral droit** `openEdtSettingsDrawer()` (showGeneralDialog slide) ; `EdtSettingsView` passé en StatefulWidget : **segment** (Salles/Trame horaire/Disponibilités) + `IndexedStack` — plus aucun `TabBar`. Largeur 560 (ou plein écran <620).
- Créneau créé/édité = **MODAL** `_SlotForm` (inchangé) ; détail créneau = **TIROIR** `_SlotDrawer` (inchangé).

**Fichiers** (tous ≤500) : `emploi_du_temps_screen.dart` (orchestrateur 450) + parts `_grid`/`_form`/`_widgets`/`_overview` (= header : KPI+filterbar+segment+chip, repensé)/`_calendar` (master calendar)/`_schedule_drawer` (slot drawer)/`_actions` (NOUVEAU : extension `_EdtActions on _EdtPageState` = modal/delete/drawer/PDF). **SUPPRIMÉ** : `emploi_du_temps_schedule.dart` (logique absorbée), `_edtFocusClassProvider` (jump interne via setState). analyze 0 / build linux ✓. **VÉRIFIÉ GUI RÉEL 2026-06-28** (directrice Aline M.) : 4 vues OK (Établissement=master calendar ; Classe=titre « Choisir la classe », cascade cycle→niveau→classe, scope chip, bouton « + Ajouter un créneau », tuile supprimable, cellules vides « + », KPI entité ; Enseignant=picker+grille lecture ; Salle) ; tiroir Paramètres segmenté (Salles/Trame/Dispo) OK ; modal trame OK. ⚠️ DPR flottant 1↔2 (iMac HiDPI) → coords xdotool à ×2 selon le backing du moment.

**Itération 4 — DURCISSEMENT cohérence ERP (2026-06-28, non commité, build+analyze 0, VÉRIFIÉ GUI)** : après audit critique (user « es-tu certain que c'est complet et parfait ? »), résolu les incohérences + complété aux standards Pronote/Untis. Périmètre **P0+P1+P2** :
- **P0-1 Salle = registre** : le champ texte libre est remplacé par un **dropdown des salles** (`rooms`) dans le form ; `timetable_slots.room_id` (FK) écrit ; conflit de salle fiabilisé par id (`_sameRoom` id-prioritaire, repli texte legacy) ; **contrôle capacité** (effectif classe > capacité salle → avertissement ambre `_CapacityNote`). `room` texte gardé pour affichage/PDF.
- **P0-2 Conformité programme** : `classRequiredHoursProvider` (somme `class_subjects.weekly_hours` par classe) ; panneau couverture renommé **« Conformité des emplois du temps »**, métrique `ok = heures placées ≥ requises` (« Programme OK ») ; **carte `_ProgramCompliance`** par classe (chips matière vert/ambre/rouge « placé/requis h », `_SubjectVolumeChip`).
- **P0-3 Trame = canevas** : `_TimetableGrid` prend `trame: List<SchoolPeriod>` (cycle de la classe) → lignes = cours trame ∪ créneaux, **cases vides « + » même classe vide**, **bandes récré/pause** pleine largeur.
- **P0-4 Permissions** : édition/suppression de créneau gâtées séparément `update`/`delete` (plus `create`) — grid `canAdd`/`canDelete`, drawer `onEdit`/`onDelete` conditionnels.
- **P1-5 Publication** : `timetable_versions` actionnable — `ensureActiveVersionId` (crée brouillon à la 1re écriture, attache `version_id`), `publishTimetableVersion`/`unpublishTimetableVersion`, **bandeau `_PublicationBar`** (Publié↔Brouillon, gate `update`+!readOnly, alerte si conflits).
- **P1-6 Disponibilités** : table `teacher_availability` branchée — `teacher_availability_provider` (modèle+stream+CRUD+`availabilityViolation`) ; **onglet Disponibilités** réel (`edt_availability_tab.dart` : déclarer indispo/préférence par prof/jour/plage, groupé, supprimable) ; le form **bloque** sur `unavailable` (DUR, jour ignoré) et **avertit** sur `preference_against` (`_AvailNote`) ; **charge max** prof `kTeacherMaxWeeklyHours=27` → KPI Heures rouge « Surcharge » en vue Enseignant.
- **P2-7 Productivité** : `duplicateClassTimetable` (dupliquer EDT vers une autre classe), `clearClassTimetable` (vider) via **menu ⋯ `_ClassActionsMenu`** en vue Classe.
- **P2-8 Livret PDF** : `EmploiDuTempsPdfService.buildBooklet` (une page/classe) ; bouton PDF en vue Établissement → **livret toutes classes**.

**Fichiers** (tous ≤500) : screen 465 + parts `_actions`(380, +resolve/duplicate/clear/publish)/`_panels`(NOUVEAU : compliance+menu)/`_grid`(trame)/`_form`(salle+capacité+dispo)/`_overview`(+PublicationBar)/`_widgets`(+CapacityNote/AvailNote/workload) ; `timetable_provider`(422)+part `timetable_conflicts`(NOUVEAU) ; `teacher_availability_provider`(NOUVEAU) ; `edt_availability_tab`(NOUVEAU) ; `emploi_du_temps_pdf_service`(+buildBooklet). **VÉRIFIÉ GUI** (directrice) : publication bar publié+Brouillon, panneau Conformité « 0 programme ok·0% », carte conformité (info si pas de weekly_hours), grille trame avec cases « + », bouton Ajouter + menu ⋯. ⚠️ **Conformité affiche 0%** tant que `class_subjects.weekly_hours` non saisi (à renseigner page Matières). Schéma local déjà OK (room_id/version_id/teacher_availability présents). Migrations 0017/0018 déjà prod ; sync-rules à vérifier pour `teacher_availability`/`timetable_versions` (gating offline).

**Cahier de textes FINALISÉ + rendu cohérent (2026-06-28, non commité, build linux ✓ / analyze 0)** : la catégorie **Enseignement** = `niveaux`/`classes`/`matieres`/`programmes`/`emploi-du-temps`/`cahier-textes` (cf `module_routes.dart`, PAS Scolarité). Page `/user/cahier-textes` (table `lesson_entries`, VÉRIFIÉE live : colonnes id/group/school/class/subject/staff/year/**trimester_id**(inutilisé)/entry_date/lesson_title/content/objectives/homework/resources ; PAS de `slot_id` ; RLS 0019 ; schema+sync-rules OK) déjà au design plateforme (KPI/ScopeDrilldownPanel/filtres/liste/détail 4 sections/CRUD/permissions). **Cohérence ajoutée dans le FORMULAIRE** (`cahier_textes_form.dart` 491) : (1) **Matière limitée au programme de la classe** (`classProgramProvider`, repli toutes matières + note si programme vide) ; (2) **enseignant auto-rempli** depuis l'affectation (`teacher_subjects`, écrasable, flag `_teacherTouched`) ; (3) **`_SeancePicker`** = puces des séances de l'EDT pour la classe+jour (`timetableSlotsProvider` filtré `classId`+`dayOfWeek==date.weekday`) → un clic pré-remplit matière+prof+titre (lien EDT↔Cahier sans migration, `_pickedSlotId` cosmétique). Changer de classe réinitialise matière/prof. Chaîne cohérente : Niveaux→Classes→Matières(`class_subjects`.weekly_hours, éditable via menu « Coefficient / horaire »→`_AssignmentEditDialog`, lu par conformité EDT)→EDT→Cahier.

**Itération 5 — bascule Jour / Semaine (2026-06-28, non commité, build+analyze 0)** : après audit honnête (user « la vue par jour/semaine/mois/trimestre/semestre/annuel + historique + drag&drop, est-ce que tout y est ? »). **Constat livré franchement** : drag&drop ✅ (déjà là, `Draggable`/`DragTarget` cellule + `_moveSlot`), vue Semaine ✅ (grille de référence), vues par ENTITÉ ✅ (`TtView` Établissement/Classe/Enseignant/Salle) ; MAIS jour/mois/trimestre/semestre/annuel ❌ et historique ❌ absents. **Point de fond** : un EDT scolaire est une **trame hebdomadaire récurrente** — jour/semaine = vues naturelles de la trame (faisable tout de suite), mais mois/trimestre/semestre/annuel n'ont de sens qu'avec une **couche d'occurrences** (projection de la trame sur le calendrier réel : dates, vacances/fériés Congo, cours annulés/déplacés ponctuels) = modèle besoin→trame→occurrence du dossier fonctionnel, **non implémenté**. **Plan validé par user (« oui ») : (1) Jour/Semaine maintenant ; (2) occurrences+calendrier réel+historique = prochaine grande vague.**
- **(1) FAIT** : `enum TtSpan { jour, semaine }` ; état page `_span` (défaut semaine) + `_focusedDay` (défaut aujourd'hui clampé Lun-Sam) ; getter `_visibleDays` ; `_TimetableGrid` ET `_MasterCalendar` prennent désormais `days: List<int>` (remplace le `static const _days=[1..6]`) ; nouveau **`_DaySpanBar`** (overview part) = segment Jour/Semaine + puces jour Lun-Sam (mode Jour, pastille « aujourd'hui »). S'applique aux deux vues (Établissement + entité).
- **(2) EN COURS — sous-étape « Calendrier scolaire d'abord » (choix user, 2026-06-28, build+analyze 0)** : audit base live AVANT de créer → `academic_years`(start/end/is_current) + `trimesters`(trimester_number/start/end/is_current, 3 lignes) DÉJÀ en base ET déjà synchro offline (bucket `by_group`) ; `school_periods` = trame. **Seul manquait** = jours non ouvrés. **Créé : `school_holidays`** (migration **0020** APPLIQUÉE PROD ; colonnes group/school/academic_year/label/kind(`ferie`|`vacances`)/start_date/end_date ; RLS tenant calquée `school_periods` ; 3 index). Ajouté à `powersync_schema.dart` + **sync-rules `by_school`** (⚠️ **à redéployer au dashboard PowerSync Cloud / PAT** sinon pas de sync offline). Provider `school_holidays_provider.dart` (modèle+stream année active+CRUD offline+helpers `isNonWorkingDay`/`holidayOn`/`covers` pour la projection à venir+**`seedCongoHolidays`** = fériés nationaux à date fixe calés sur [yearStart,yearEnd], anti-doublon ; fériés mobiles Pâques & vacances = saisie manuelle). UI **onglet « Calendrier »** ajouté au tiroir Paramètres EDT (segment 4 items) : `edt_calendar_tab.dart` (résumé fériés/vacances/jours-off, bouton « Fériés du Congo », form ajout type+libellé+plage de dates bornée à l'année, groupé vacances/fériés, suppression). **PAS encore vérifié GUI.**
- **(2b) VUES CALENDAIRES PROJETÉES — FAITES (2026-06-28, build+analyze 0, PAS encore GUI)** : empan `TtSpan` étendu à **jour/semaine/mois/trimestre/semestre/annuel** (`isCalendar` = mois+). `_SpanBar` (part `emploi_du_temps_spanbar.dart`) = segment 6 empans + sous-sélecteur contextuel (puces jour ; ◀ mois ▶ ; puces T1/T2/T3 ; S1/S2). **`_CalendarProjection`** (part `emploi_du_temps_projection.dart`) projette la trame hebdo sur le calendrier réel d'une période [from,to] : balaye chaque date, EXCLUT dimanches + `school_holidays` (fériés/vacances via `holidayOn`), calcule **jours ouvrés / séances projetées / HEURES projetées** (= vrai volume période, pas juste hebdo) + mini-mois (colonnes Lun→Sam, heatmap séances/jour, fériés/congés teintés, aujourd'hui cerclé) + (vue Classe) **volume horaire projeté PAR MATIÈRE**. Bornes via `edtSpanRange` (mois=mois courant borné année ; trimestre=`trimesters` start/end, repli tiers ; semestre=moitié de l'année par date ; annuel=année). `trimestersProvider`+`activeYearProvider` watchés. Refactor tailles : extrait `emploi_du_temps_body.dart` (`extension _EdtBody on _EdtPageState`, +méthode passe-plat `mutate()` pour éviter le lint setState-hors-State) + `emploi_du_temps_spanbar.dart` → screen 363/projection 511(cohésif)/overview 463. **Reste (2c)** : couche d'occurrences/exceptions (cours annulé/déplacé ponctuel) + journal d'audit des modifs EDT.
- **✅ DÉPLOYÉ 2026-06-28** : migration 0020 appliquée prod + sync-rules `school_holidays` **déployées au Cloud live** (instance Development) via CLI `deploy sync-config --sync-config-file-path=…/config/sync-rules.yaml` ; vérifié par `fetch config` (live = fichier édité, +1 table, 0 retrait). Au passage : clarifié que le Cloud utilise `bucket_definitions` (pas le `sync-config.yaml` streams, mort) → voir [[sync-config-divergence]]. **Reste pour finir l'EDT** : (2c) occurrences/exceptions + audit ; puis GUI test des vues calendaires ; puis Évaluation.

- **(2c) COUCHE D'EXCEPTIONS — FAITE + DÉPLOYÉE (2026-06-28, build+analyze 0, PAS encore GUI)** : table **`timetable_exceptions`** (migration **0021** appliquée prod ; slot_id nullable, exception_date, kind `cancelled`/`moved`/`extra`, new_start/end_time, new_room_id/staff_id/subject_id/class_id, reason, created_by ; RLS tenant ; 4 index) — **sync-rules DÉPLOYÉES au Cloud** (by_school, vérifié `fetch config` : live = fichier édité, toutes tables alignées). Provider `timetable_exceptions_provider.dart` (modèle+stream année active+joins libellés+CRUD+`exceptionsByDate`). **Projection intègre les exceptions** : balayage par date applique annulations (−séance si slot dans la sélection), séances exceptionnelles (+séance si classe concernée), déplacées (neutre) → séances/heures EFFECTIVES + KPI « Séances annulées »/« Séances en plus » + pastille orange sur la case-jour. **Vue Mois : case-jour ouvrée CLIQUABLE** → tiroir `_DayExceptionsSheet` (parts `_daydetail`+`_extraform`) : séances prévues de la trame ce jour (Annuler/Rétablir), séances exceptionnelles (supprimer), bouton « + Séance exceptionnelle » (form `_ExtraSessionForm` : matière du programme+prof auto, début/fin, salle option, motif) — gaté `create`/`delete`, classe requise pour 'extra'. Fichiers re-scindés : `_minimonth` + `_extraform` extraits → tous ≤500.

- **(2d) JOURNAL D'AUDIT EDT — FAIT + DÉPLOYÉ (2026-06-29, build+analyze 0, PAS encore GUI)** : choix = **trigger serveur** plutôt qu'instrumenter chaque mutation Dart. Migration **0022** (appliquée prod) : fonction `log_edt_audit()` SECURITY DEFINER (skip si `auth.uid()` NULL → ne bloque jamais seed/système ; capture l'acteur réel même pour les écritures offline au moment de la remontée PowerSync sous le JWT de l'agent) + triggers AFTER I/U/D sur `timetable_slots`/`timetable_exceptions`/`timetable_versions` → écrit dans la table `audit_logs` existante (action/table_name/record_id/old_values/new_values jsonb). **Sync offline LIMITÉE EDT** : `audit_logs` ajoutée au schéma PowerSync + sync-rules `by_school` = **3 SELECT séparés à `=`** (un par table EDT) car PowerSync n'accepte PAS `IN (liste)` en data-query (seulement `=` ou `IN (SELECT…)`) — data-minimization : l'audit finance/discipline ne descend PAS offline. **DÉPLOYÉ** (validation OK, `fetch config` : live=fichier édité, audit_logs inclus). Provider `edt_audit_provider.dart` (db.watch 200 dernières, join acteur `profiles`, résumé lisible depuis jsonb : jour/heure créneau, kind+date exception, statut version). UI : **tiroir « Historique »** (`emploi_du_temps_history.dart` standalone, bouton ⏱ dans la barre de filtres) — liste chrono, icône colorée par action (vert ajout/rouge suppr./bleu modif), acteur + « il y a N min ». ⚠️ Test psql impossible (auth.uid() NULL en psql → trigger skip volontairement) ; valable seulement via l'app authentifiée.

**Reste Enseignement** : **GUI test** (vues calendaires/exceptions/historique pas encore vérifiées visuellement). Puis **Évaluation** (Notes/Bulletins/Conseils). EDT désormais fonctionnellement COMPLET : jour/semaine/mois/trimestre/semestre/annuel + historique + drag&drop + exceptions + calendrier + conformité + publication + dispos + salles + PDF. Dette : `subjects_parts.dart`=774 lignes (>500, pré-existant, à scinder quand touché). Puis **Évaluation** (Notes/Bulletins/Conseils). Voir [[scolarite-pages-classes-matieres-eleves]] pour le design partagé (KPI/PDF/ScopeDrilldownPanel).
