---
name: communication-media-compression
description: "Compression images/vidéos à l'upload (effet WhatsApp) + fix RLS création de groupe + décision anti-MUX"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9a353d1e-8580-4225-ac0f-9f5e98c64bd7
---

**Livré 2026-06-14** — durcissement médias & temps réel communication (pages partagées par TOUS les rôles).

---
## Notes vocales — fiabilisation 2026-06-15
**Symptômes** : (1) « la lecture audio ne fonctionne pas » ; (2) « ça ne se lit qu'une fois, la relance ne marche pas ».
**Diagnostic (vérifié en ligne de commande)** : record_linux = `parecord` (PCM s16le) → pipe → `ffmpeg` (AAC). Le pipeline est SAIN (2,5 s → m4a 30 Ko valide ; GStreamer `avdec_aac`+`qtdemux` décode, exit 0). La vraie cause #1 = des captures VIDES (note en base = **44 octets** : micro relâché avant le 1er buffer) → fichier sans flux → « no known streams ». Cause #2 = `audioplayers` en `ReleaseMode.release` (défaut) **libère la source après la 1ʳᵉ lecture** → `resume()` ne rejoue rien.
**Corrections** :
- `audio_recorder_button.dart` : garde-fou taille `bytes.length < 2000` → on n'envoie PAS une capture vide (message « Aucun son capté »). Le garde-fou temps (`<800 ms`) restait insuffisant.
- `audio_message_player.dart` : (a) **télécharge** les octets du Storage privé puis joue depuis un **fichier temp local** (`setSourceDeviceFile`) — bien plus fiable que le streaming HTTP signé sur Linux/GStreamer ; détecte aussi le fichier vide (`<1500` octets) → état « Note vocale indisponible » + bouton réessayer ; cache `cacheKey→tempPath` pour relecture instantanée. (b) **`setReleaseMode(ReleaseMode.stop)`** (en initState ET au début de `_toggle`, idempotent → s'applique même à une instance déjà créée après hot reload) + `seek(0)` à la fin de lecture et avant une relance si la tête est en fin de piste → **rejouer marche**.
**Règle** : sur desktop Linux, lire l'audio depuis un fichier local téléchargé, pas via URL signée streamée ; et toujours `ReleaseMode.stop` pour autoriser la relecture. Voir [[communication-receipts-presence]].

**Compression upload (« effet WhatsApp »)** : `features/communication/widgets/media_compression.dart`.
- Images → pur Dart `image` (universel, marche sur Linux dev) : resize ≤1600px + JPEG q80, exécuté en isolate `compute()` (pas de jank). Photo 8 Mo → ~400 Ko.
- Vidéos → `video_compress` (Android/iOS only, gardé par `videoCompressionSupported`) : MediumQuality ~720p. Ailleurs (desktop/web) = original.
- Point d'entrée unique `compressForUpload()` branché dans `pickAndUploadAttachments()` (comm_attachments.dart) AVANT le contrôle des 25 Mo → partagé par les 5 composeurs (messagerie, thread, annonces, événements, stories).

**Décision archi : PAS de MUX.** MUX = streaming, incompatible avec l'offline-first (cœur école). Le vrai gain WhatsApp = compression à la source (fait) + ~~FCM push~~ ⚠️ **PÉRIMÉ le 2026-08-29 : Firebase est SORTI du projet** — le canal est la table `notifications` + PowerSync + la cloche, voir [[powersync-status]] et le garde `notification_sans_firebase_test.dart` + buckets privés/signed URLs (à faire — actuellement `getPublicUrl` = pièces jointes publiques, faille confidentialité). MUX/Cloudflare Stream seulement si un jour vidéo longue broadcast online (Phase 2).

**Fix création de groupe (RLS)** : migration `fix_group_creation_member_insert_rls`. La policy `conv_members_insert` exigeait `is_conversation_member()` qui ne voit pas la ligne du créateur dans le même INSERT batch → tous les autres membres rejetés. Ajout fonction `is_conversation_creator(conv_id)` + policy l'autorise. Code `createGroupConversationScoped` réordonné : créateur d'abord, puis les autres.

**Rendu instantané** : `sendMessageScoped`/`sendGroupMessageScoped`/`createGroupConversationScoped` font `ref.invalidate(messagesProvider/groupConversationsProvider)` après envoi online (pas d'attente du round-trip Realtime). Debounce Realtime messages 1s → 400ms.

**Storage privé + signed URLs (sécurité, livré 2026-06-14)** : migration `secure_communication_attachment_buckets`. Buckets `message-attachments`/`communication-attachments` passés en **privé** (étaient world-readable via getPublicUrl = faille docs médicaux/paie). RLS storage.objects scopée par groupe : SELECT = `is_super_admin() OR owner=auth.uid() OR foldername[1]=auth_group_id()` ; INSERT idem. `MessageAttachment` porte `path`+`bucket` ; upload stocke un signed URL 1 an (fallback+cache offline) ; affichage re-signe via `resolveAttachmentUrl()` (cache mémoire 50 min) + widget `SignedNetworkImage` (cacheKey=path → image déjà vue reste offline malgré rotation URL). Legacy public URLs : `path` re-déduit de l'URL (`_parse`). ⚠️ L'upload doit cibler le **dossier = groupe du destinataire** (compose plateforme = option=group id ; thread = conv group) sinon le destinataire ne peut pas lire. Vérifié : PDF legacy s'affiche toujours sous bucket privé.

**Actions chat WhatsApp (livré 2026-06-14)** : appui long / clic droit sur une bulle → menu **Copier** / **Supprimer** (`deleteMessageScoped`, mes messages only). Images chat agrandies (thumbSize 210). FAB speed-dial Nouveau message/groupe + modals vérifiés (annuaire plateforme = school_groups).

**Tests GUI complets 2026-06-15 (tout vérifié à l'écran)** : upload image simple + **multi-sélection (4 d'un coup)** → compression PROUVÉE en direct (photo 2,1 Mo → 330 Ko via resize 1600+JPEG q80) → affichage grille via signed URLs → lightbox plein écran ; PDF legacy s'affiche sous bucket privé ; Copier (snackbar « Message copié ») ; Supprimer (dialog confirmation → message supprimé). **2 BUGS super_admin trouvés & corrigés** : (1) création groupe échouait — `conversations.group_id` NOT NULL alors que `me.groupId` null pour super_admin → fix `createGroupConversationScoped` branche plateforme : résout chaque groupe choisi vers son admin (`groupAdminRecipient`) + ancre la conv au 1er groupe (group_id valide) ; (2) envoi message groupe échouait — `invalid input syntax for type uuid ""` car le thread retombait sur `widget.groupId` (vide) pour un groupe sans messages → fix getter `_convGroup` lit `conv.group.groupId` d'abord. Build ✓ analyze 0 ; groupe créé vérifié (group_id valide en base).

**Paramètres de groupe + suppression (livré 2026-06-15)** : nouveau `screens/group_settings_dialog.dart` (« Infos du groupe » style WhatsApp) ouvert au tap sur l'en-tête du fil de groupe ou l'icône ⓘ. Admin = créateur (`createdBy==myId`). Fonctions dans group_chat_provider.dart : `renameGroupConversationScoped`, `updateGroupAvatarScoped` (photo de groupe → réutilise `pickAndUploadAttachments`+compression, stocke l'URL signée 1 an dans `conversations.avatar_url`), `addGroupMembersScoped` (**renvoie le nb réellement ajouté** ; plateforme = résout chaque groupe→admin via `groupAdminRecipient`, upsert idempotent onConflict conversation_id,user_id), `removeGroupMemberScoped` (= quitter si self), `deleteConversationScoped` (créateur ; migration **`conversation_delete_policy`** = nouvelle policy `conv_delete` USING super_admin OR created_by=uid ; FK conversation_id ON DELETE CASCADE purge membres+messages ; école = purge locale db.execute), `deleteDirectConversationScoped` (1-à-1 = supprime tous les messages, RLS msg_delete sender|recipient). Dialog **réactif** (`ref.watch(groupConversationsProvider)` → reflète renommage/avatar/membres sans rouvrir). Header de fil : avatar groupe via nouveau widget partagé `GroupAvatarImage` (photo CachedNetworkImage ou pastille dégradée ; remplace l'ancien `_GroupAvatar`) ; menu ⋮ 1-à-1 = « Supprimer la discussion ». ⚠️ Réalité data : 1 SEUL groupe a un admin_groupe actif (Ministère/vianney `da3954ca`) → côté super_admin, la plupart des groupes ajoutés résolvent 0 membre (message honnête affiché).

**Viewers confinés au contenu — sidebar TOUJOURS visible (livré 2026-06-15)** : exigence user = images/PDF/vidéo ne doivent PAS recouvrir la sidebar. Mécanisme dans `core/widgets/app_shell.dart` : `contentOverlayProvider` (StateProvider<Widget?>) + helpers `showContentOverlay(context, (close)=>widget)` / `closeContentOverlay` (lisibles depuis n'importe quel BuildContext via `ProviderScope.containerOf`). La zone de contenu d'AppShell est un `Stack([widget.child, if(overlay!=null) Positioned.fill(overlay)])` → le viewer occupe SEULEMENT le contenu, la sidebar reste visible ET cliquable. Auto-fermeture au changement de route (`_lastLoc` + postFrame reset). `openImageLightbox`, `openPdf` (media_viewer.dart) et `openVideo` (feed_video_player.dart) passent par `showContentOverlay` au lieu de Navigator.push/showDialog (qui couvraient toute la fenêtre). PDF = `_PdfOverlay` carte centrée ≈94% du contenu (max 1100px) sur backdrop, clic à côté ferme ; lightbox = `Material` noir confiné ; vidéo = backdrop confiné. Linux dev : PDF/vidéo non supportés → externe (images OK). **Vérifié GUI 2026-06-15** : lightbox image s'ouvre dans le contenu, sidebar pleine visible, navigation sidebar pendant viewer ouvert → auto-close. ⚠️ media_viewer.dart & feed_video_player.dart importent désormais app_shell.dart (pas de cycle).

Vérifié GUI 2026-06-15 (hot restart, 0 runtime error) : dialog « Infos du groupe » rendu parfait (avatar+caméra, nom+crayon, membre badge Admin, Ajouter des membres ouvre l'annuaire scope-aware, Supprimer le groupe). analyze 0 / build ✓.

**Actions chat WhatsApp avancées (livré 2026-06-15)** — couche données + UI complètes, scope-aware (offline/online) :
- **Réactions emoji** : colonne `messages.reactions jsonb` (migration `messages_reactions_edited`) + `Column.text('reactions')` au powersync_schema (SELECT * propage). `parseReactions()`. `toggleReactionScoped(ref, msg, emoji, myId)`. ⚠️ Migration `msg_update_allow_conversation_members` : `msg_update` élargie aux **membres de conversation** (sinon en groupe recipient_id NULL → seul l'expéditeur pouvait réagir ; nécessaire online ET sync offline). Menu bulle = rangée 6 émojis (👍❤️😂😮😢🙏) ; pastilles sous la bulle (surlignées si j'ai réagi). **Vérifié** : réaction écrite en base + chip rendu.
- **Réponse citée** : `parentId` ajouté à `sendGroupMessageScoped` ; en 1-à-1 `parentId` n'est mis QUE pour une vraie réponse (sinon null — le regroupement se fait par interlocuteur ; les anciens msgs avaient parentId=racine → exclus du rendu cité). Aperçu cité `_QuotedPreview` dans la bulle + `_ComposeContextBar` au-dessus de l'input.
- **Édition** : colonne `messages.edited_at` ; `editMessageScoped` ; indicateur « modifié » ; bouton ✓ remplace ➤ en mode édition (mes msgs texte only).
- **Transfert** : `ForwardMessageDialog` (part `messagerie_staff_forward.dart`) → personnes (annuaire) + groupes ; réutilise send.
- **Recherche intra-fil** : loupe header → `_searchQuery` filtre + surlignage jaune dans `_Bubble`.
- **Menu contextuel** : appui long/clic droit → Réagir/Répondre/Transférer/Copier/Modifier/Supprimer (⚠️ piège résolu : `PopupMenuItem(enabled:false)` BLOQUE les taps enfants → retirer `enabled:false` pour la rangée émoji).
- Bulle extraite dans nouveau part `messagerie_staff_bubble.dart` (règle 500 lignes).

**Filtre Groupes + barre de saisie WhatsApp + note vocale (livré 2026-06-15)** :
- Onglet **« Groupes »** (compteur) dans le tri Tous/Non lus/Groupes/Archivés (filtre `'groups'`). Avatar de groupe = photo (déjà via GroupSettingsDialog).
- Barre de saisie séparée : 😊 emoji · champ · 📎 **document** (`_attach()`) · 🖼️ **photo** (`_attach(imagesOnly:true)` ; `pickAndUploadAttachments(imagesOnly)`) · ➤/🎤. Pièces jointes en attente = **miniatures prévisualisables** (tap → lightbox/PDF) avec bouton retrait (`_PendingChip` dans comm_attachments).
- **Note vocale façon WhatsApp** : `widgets/audio_recorder_button.dart` (appui maintenu → enregistre, relâche → envoie, glisse gauche → annule ; chrono + bandeau `AudioRecordingBanner`). Le bouton micro GARDE le geste (stable), le parent rend le bandeau. Package **`record: ^7.1.0`** (⚠️ `record` 5.x CASSE le build Linux : record_linux 0.7.2 vs interface 1.6.0 incompatibles → monter à 7.x = record_linux 2.1.0 cohérent). Bouton ➤ si texte/pièces/édition, sinon 🎤. **Vérifié build Linux ✓** ; sur ce poste Linux sans périphérique audio → fallback propre « Enregistrement vocal indisponible sur cet appareil » (sur mobile = enregistre). ⚠️ saisie clavier xdotool n'atteint PAS les TextField Flutter Linux (IME) — tester le texte autrement.

**Notifications 100 % Supabase (livré 2026-06-15, décision user : zéro Firebase)** : trigger `trg_notify_on_message` (migration `notify_on_message_trigger`, fonction SECURITY DEFINER `notify_on_message()`) → AFTER INSERT messages crée 1 ligne `notifications` par destinataire (1-à-1) ou par membre du groupe hors expéditeur (titre = nom groupe, body = « expéditeur : aperçu »). Livré via **Realtime** (online) + **PowerSync sync-rules** `recipient_id=bucket.uid` (école offline). ⚠️ Cloche rendue **personnelle** : `notificationsProvider` online filtre désormais `.eq('recipient_id', myUid)` (sinon super_admin voyait TOUTES les notifs via RLS) ; idem `markAllNotifRead`. **Vérifié** : message de Léon → badge cloche « 1 » + dropdown « Coordination pédagogique 3e — Léon Bouanga : Réunion demain… » chez Ramsès. Pas de push OS app fermée (impossible sans FCM/APNs) — assumé.

**« En train d'écrire » (livré 2026-06-15, Realtime Broadcast)** : canal `typing:<key>` (key = `conv_<id>` groupe ou `dm_<uid1_uid2 triés>`), broadcast `{user_id,name,typing}`, throttle 3 s, auto-clear 5 s. Affiché sous le nom dans le header du fil (vert italique). Online uniquement (broadcast = pas de DB, pas de RLS). Imports : `messagerie_staff.dart` + `dart:async` + `supabase_flutter show RealtimeChannel, RealtimeChannelConfig`. Compile/run OK (test visuel complet = 2 clients).

**PDF in-app TOUTES plateformes + cache (livré 2026-06-15)** : Syncfusion (pas de Linux → ouvrait en externe) remplacé par **`pdfrx` (pdfium)** dans `media_viewer.dart` — `PdfViewer.uri` rend le PDF dans le panneau confiné (sidebar visible) sur Android/iOS/Web/Windows/macOS/**Linux**, avec cache pdfium intégré (réouverture instantanée). `_pdfSupported` supprimé (toujours in-app). `errorBannerBuilder` = repli propre (jamais d'écran rouge) → message + ouverture externe. **Vérifié à l'écran** : reçu PDF affiché in-app sur Linux. ⚠️ pdfrx auto-init (pas de main). Le `TimeoutException` initial venait du crash `dispose` (voir ci-dessous), pas de pdfrx.

**Cache contenu chat (livré 2026-06-15)** : `cachedSignedUrl(att)` (lookup synchrone du cache `_signedUrlCache`) ; `SignedNetworkImage` rend directement `CachedNetworkImage` si l'URL est déjà en cache → un contenu déjà ouvert ne re-signe plus / ne ré-affiche plus de spinner. messagesProvider garde déjà `keepAlive`.

**Sélection texte chat (livré 2026-06-15)** : `SelectionArea` autour du ListView du fil → texte des bulles sélectionnable/copiable (glisser souris) ; l'appui long garde son menu.

**⚠️ BUG corrigé** : `_ThreadView.dispose()` utilisait `ref.read(...)` après dispose (interdit en ConsumerState) → crash « Cannot use ref after disposed » qui corrompait l'arbre. Fix : `_typingChannel?.unsubscribe()` (pas de `ref`).

**Audio note vocale — RÉSOLU (livré 2026-06-15)** : le code `record` était bon ; l'échec « indisponible » venait de l'ENVIRONNEMENT Linux. `record_linux` capture via **`parecord` (PulseAudio)** puis encode via **`ffmpeg`** (TOUS les encodeurs passent par ffmpeg). `ffmpeg` était MANQUANT → `start()` levait. **Installé `ffmpeg`** (sudo `‹secret — gestionnaire de mots de passe›`) → enregistrement de bout en bout OK (**vérifié en direct** : « Note vocale.m4a » audio/mp4 33 Ko envoyée+affichée). Sur mobile (cible) = encodeurs natifs, aucune dépendance. **Lecteur audio INLINE** : `widgets/audio_message_player.dart` (`AudioMessagePlayer`, play/pause + Slider progression + chrono, 1 seul lecteur actif) via **`audioplayers: ^6.7.1`** ; branché dans `CommAttachmentView` (branche `a.isAudio`, param `mine`). ⚠️ audioplayers sur Linux = **GStreamer** : installé `libgstreamer1.0-dev` + `libgstreamer-plugins-base1.0-dev` (build Linux ✓). Lecteur compile+build ; click-test GUI non rejoué (sidebar xdotool ne défilait plus dans la session fraîche).

Reste à faire (priorisé) : accusés lecture/compteur non-lus groupe ; présence en ligne. ~~FCM~~ exclu (Supabase-only) ; ~~PDF externe~~, ~~audio~~, ~~lecteur inline~~ faits. ; lecteur audio INLINE (actuellement note vocale = chip « Écouter » → externe) ; accusés lecture/compteur non-lus en groupe ; présence/typing. Voir [[realtime-publication-requirement]], [[communication-unification-plan]].
