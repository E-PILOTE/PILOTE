---
name: poste-vitrine-securite-refonte
description: "Refonte écran-verrou poste partagé — vitrine de sécurité + profils animés + durcissement PIN + mode d'appareil (Phase 1 livrée)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 66c7bd66-1eb0-4744-a41e-b4efd39667b6
---

Refonte de l'entrée du poste scolaire partagé (2026-07-06). Spec : `docs/superpowers/specs/2026-07-06-poste-partage-vitrine-securite-design.md`. Étend [[ecran-verrou-poste-partage]] et [[poste-partage-agent-switch]].

**Décision structurante** : l'écran-verrou a désormais DEUX états — **vitrine de sécurité** au repos (co-branding MEPSA·METP+école, horloge sobre, bouton unique « Ouvrir une session », pied E-PILOTE, tricolore ; JAMAIS de scroll) → **feuille bleu nuit** qui monte en fondu (profils puis PIN, défilement INTERNE au modal). La vitrine EST la surface éditoriale/commerciale du parc national.

**Phase 1 LIVRÉE (100 % front, 0 backend, non commité) :**
- **Mode d'appareil** `DeviceMode {shared, personal}` (`deviceModeProvider`, persisté SharedPreferences, choix au 1ᵉʳ lancement via `DeviceModeScreen`, modifiable dans Paramètres›Sécurité `DeviceModeTile`). Poste **personnel** = AUCUN verrou (l'utilisateur EST l'agent) ; corrige le défaut « verrou toujours imposé ». `computeNeedsAgentUnlock` gagne `deviceMode`+`forcePicker` ; `computeNeedsDeviceModeChoice` + `needsDeviceModeChoiceProvider` ; gate affiche mode→verrou→rien. « Changer d'utilisateur » (header) pose `forceAgentPickerProvider=true` (bascule à la demande même en perso).
- **Durcissement PIN** (fixé **4 chiffres**) : auto-validation au 4ᵉ, **clavier physique** (0-9, ⌫, Entrée, Échap — poste desktop), **secousse** à l'erreur, bouton « Afficher », **cooldown progressif persistant** (`pinCooldown` pur : 5ᵉ échec 30s→60s→2min→5min plafond ; `recordFail/lockedUntil/clearFails`). `setPin` écrit `agent_pin_set_at` (préparation reset Phase 2).
- **Récents** (`recordUsage/recentIds` SharedPreferences) — rangée « Récemment utilisés » dans le modal.
- Fichiers : `vitrine_shell.dart`, `vitrine_clock.dart`, `agent_keypad.dart` (nouveaux) ; `agent_lock_screen.dart`, `agent_grid.dart`, `agent_pin_pad.dart`, `agent_lock_gate.dart`, `device_mode_screen.dart`, `device_mode_setting.dart` (refondus) ; `active_agent_provider.dart` (étendu). Tous ≤388 l.
- **Vérif** : 34 tests (logique pure + service + flux widget + 4 goldens `test/goldens/lock_*.png`), analyze 0, build Linux OK. Goldens inspectés visuellement : vitrine/profils/PIN/mode OK. ⚠️ police de test = texte en blocs ; capturer APRÈS l'anim cascade (sinon opacité 0).

**Phase 2 LIVRÉE (code commité `7b6e931`, NON déployée) :** reset PIN par admin_groupe. Migration `0033_agent_pin_reset.sql` (`profiles.pin_reset_requested_at`, `IF NOT EXISTS`, PAS de nouvelle RLS — policy UPDATE admin_groupe existe déjà) ; sync-rules bucket `directory` +colonne ; schéma local +colonne ; `AgentOption.pinResetRequestedAt` + `pinResetInvalidates(resetAt,setAt)` pur ; `_pick` recrée le PIN si reset>set (lève aussi le cooldown) ; `AdminUsersService.resetAgentPin` + action « Réinit. code du poste » (carte popup + tableau, gatée `agentLockApplies`, confirmation). Tests OK. ⚠️ **Déploiement à faire (ordre rétro-compatible)** : appliquer mig 0033 → **redéployer sync-rules au dashboard PowerSync** → release app. Vérifier live avant (colonne absente + policy UPDATE admin_groupe). Spec `docs/superpowers/specs/2026-07-06-reset-pin-admin-groupe-phase2-design.md`.

**Phase 3a LIVRÉE (commit `f6aed49`, NON déployée) :** messages de service sur la vitrine. Migration `0034_platform_service_messages.sql` (table + RLS super_admin ALL / lecture actifs par authentifié) ; sync-rules **bucket GLOBAL `global_catalog`** +table ; schéma local +table ; `serviceMessagesProvider` (db.watch, filtré actif+fenêtre, cap 3) + `isMessageLive` pur ; carrousel auto ~7s dans `VitrineShell` (respecte `disableAnimations`) câblé via `AgentLockScreen` ; écran super_admin CRUD **« Messages d'accueil des postes »** (`platform_service_messages_screen.dart` + provider, route `superMessagesAccueil`, nav section COMMUNICATION). Tests `isMessageLive` + golden vitrine avec bandeau. ⚠️ Déploiement : mig 0034 → **sync-rules dashboard** → release. Spec `docs/superpowers/specs/2026-07-06-messages-service-vitrine-phase3a-design.md`.

**PR :** `feat/poste-vitrine-securite` = **PR #16** (Phases 1+2+3a). Décisions/discussion : login=vitrine sécurité, poste perso sans verrou, pub tierce écartée (public/scolaire), upsell→admin_groupe.

**Phase 3b LIVRÉE (commit `3886b75`, NON déployée) :** placements partenaires. Décisions (recos suivies) : opt-in par **groupe**, partenaires **globaux** v1, **aucun** flux financier, création super_admin = approbation. Migration `0035_platform_partners.sql` (table + RLS super_admin ALL/lecture actifs + colonne `school_groups.partner_display_enabled` défaut false) ; sync-rules partenaires via **global_catalog** (opt-in descend déjà via `by_group SELECT *`) ; schéma local +2 ; providers staff `isPartnerLive`/`showPartnerStrip` purs + `vitrinePartnersProvider`/`groupPartnerOptInProvider` ; rendu réel `_PartnerStrip` (logos `CachedNetworkImage` + **repli texte offline**) ; CRUD super_admin « Partenaires des postes » (`platform_partners_screen.dart` + `platform_partner_form_dialog.dart` extrait + provider + upload bucket **`partner-logos`**, route `superPartenaires`, nav) ; interrupteur opt-in admin_groupe (`PartnerOptInTile`, Paramètres›Général). Tests purs + golden `lock_vitrine_partners.png`. ⚠️ Déploiement : mig 0035 → **créer bucket `partner-logos`** → sync-rules dashboard → release.

**Toutes phases (1,2,3a,3b) COMMITÉES sur `feat/poste-vitrine-securite` = PR #16.**

**✅ DÉPLOYÉ EN PROD (2026-07-15, via `psql` direct + CLI PowerSync) :** migrations 0033+0034+0035 appliquées ; `platform_service_messages` + `platform_partners` en **`REPLICA IDENTITY FULL`** (indispensable : sinon une ligne passée `is_active=false` ne serait pas retirée des postes) ; auto-incluses dans publication `powersync` (FOR ALL TABLES) ; bucket **`partner-logos`** (public) + 4 policies storage miroir de `group-logos` ; **sync-rules déployées** sur l'instance Cloud « Development » (`npx --no-install powersync deploy sync-config --sync-config-file-path=.../config/sync-rules.yaml`, PAT `PS_ADMIN_TOKEN`). Pré-check diff live↔local = **+2 tables, 0 retrait** (additif sûr) ; post-vérif `fetch config` confirme les 2 `FROM platform_*`. Reste = **release app** (build) uniquement. cf [[sync-config-divergence]].

**🐛 BUG CRITIQUE trouvé au LANCEMENT réel + FIX (commit `8675dd6`) :** l'écran-verrou/vitrine est monté dans `MaterialApp.builder` (frère du Navigator) → **PAS d'Overlay ancêtre**. Or `Tooltip` et `TextField` (EditableText) exigent un Overlay au build → « No Overlay widget found » → **écran d'erreur rouge en prod**. ⚠️ Les golden tests NE L'ONT PAS VU car `MaterialApp(home:)` fournit un Overlay — piège général. Fix dans `agent_lock_gate.dart` : héberger l'écran dans un **Overlay STABLE** (jamais recréé), animation à l'intérieur (AnimatedSwitcher enfant) + `IgnorePointer` quand rien. Un Overlay recréé par AnimatedSwitcher externe → assertion Semantics `!child.attached`. Test de régression reproduisant le câblage `MaterialApp.builder` ajouté.

**✅ LANCÉ & VÉRIFIÉ app réelle (`flutter run -d linux`, 2026-07-06) :** 0 exception, flux vitrine→recherche→sélection profil→PIN→**dashboard directeur** OK, PowerSync en synchro live, nouveau schéma (platform_service_messages/partners) n'a pas cassé la synchro. 140 tests verts (2 admin_geo préexistants hors périmètre). Auth Supabase MCP amorcée (OAuth à compléter par l'user pour appliquer les migrations). : table messages de service + placements partenaires (CRUD super_admin, opt-in), synchro offline, carrousel+encart dans `VitrineShell` (déjà câblé via params `serviceMessages`/`showPartner`). Pub tierce ÉCARTÉE (contexte public/scolaire) ; upsell → espace admin_groupe.

⚠️ PIN 4 fixe : un ancien PIN 5-6 chiffres devient inutilisable → couvert par reset (Phase 2). Acceptable (espace perso non déployé).

**🐛🎨 Correctifs + passe UI (commit `f028d9b`, poussé PR #16, 2026-07-07) :** 3 bugs + design. (1) « Changer d'utilisateur » ne faisait RIEN → Overlay `initialEntries` figeait son contenu ; la porte ne monte plus l'Overlay QUE si `showing`, contenu frais (bascule forcée → feuille profils direct). (2) Crash au clic « Afficher » (révéler PIN) → `AnimatedContainer` animait `shape` cercle→rect (BoxDecoration interdit) ; n'animer que le rayon. (3) ~400 assertions Semantics `!child.attached`/frame à l'écran de login → Overlay+AnimatedSwitcher monté en permanence ; corrigé par (1). Détails techniques → [[overlay-builder-golden-blindspot]]. Design (demande user) : boutons/champs/cartes rayon **8 px** + couleurs plateforme (`kAuthAccent` navy, fin des bleus ad-hoc), **carte profils élargie 560 px + grille responsive** (`maxCrossAxisExtent`, noms complets), curseur main, fond **`login_bg.webp`** (login+verrou). 142 tests, analyze 0, app réelle : 0 exception à la vitrine/login (vérifié). ⚠️ interaction xdotool sur embedder Linux peu fiable (hover OK, tap synthétique flaky) — le clic « Ouvrir une session » n'a pu être piloté au clavier/souris synthétique ; couverture par tests widget fidèles.
