---
name: edt-refonte-v2
description: Refonte complète du module Emploi du temps (ERP pro) — dossier fonctionnel validé + implémentation par vagues ; Vague 0 (fondation données) posée non déployée
metadata: 
  node_type: memory
  type: project
  originSessionId: 3a1b758d-d9ce-4f8b-9d12-8481218fca0d
---

Refonte **from scratch** du module Emploi du temps en module ERP pro (remplace [[enseignement-emploi-du-temps]], jugé « bon MVP » mais trame figée). Le user a délégué TOUTE décision (« tu es seul, c'est ta plateforme, fais que ce soit parfait ») le 2026-06-28.

**Dossier d'architecture FONCTIONNELLE validé** (Parties I+II, dans l'historique conv) : analyse critique, acteurs/ressources/événements/contraintes, hiérarchie, cycles de vie, permissions, dépendances modules, états, validations, erreurs, diagrammes Mermaid, priorisation MVP→V3, exigences non-fonctionnelles, paramétrage, vision 10 ans. Décision-clé = modèle **besoin → trame → occurrence datée** ; cycle de vie porté par `timetable_versions` (brouillon→…→publié→archivé), jamais par le créneau.

**Conception technique** : `docs/EDT_ARCHITECTURE_TECHNIQUE.md` (bridge + plan de vagues + checklist déploiement). 1ère vague livrable choisie = **MVP socle**.

**Vague 0 — fondation données : FAITE (code), PAS DÉPLOYÉE.** Migrations `database/migrations/0015`→`0019` :
- 0015 `rooms` (registre salles typées, remplace `room` texte libre)
- 0016 `school_periods` (trame horaire configurable, remplace `kStdPeriods` codé en dur)
- 0017 `teacher_availability` (dispos/indispos/préférences ; absence de ligne = dispo)
- 0018 `timetable_versions` + ALTER timetable_slots ADD `version_id`,`room_id` + backfill non destructif
- 0019 RLS hardening : `timetable_slots`/`lesson_entries` n'avaient AUCUNE RLS (faille héritée) → policy tenant ajoutée
`powersync_schema.dart` + `sync-rules.yaml` (bucket by_school) mis à jour. `flutter analyze` schema = 0 issue.

**Migrations 0015→0019 : APPLIQUÉES + VÉRIFIÉES en prod le 2026-06-28** (4 tables + RLS + policies + indexes ; timetable_slots a version_id/room_id ; backfill OK = 2/2 slots liés à 1 version active 'publie'). Méthode psql qui MARCHE (cf. [[supabase-credentials]]) : pooler `aws-1-eu-central-2.pooler.supabase.com:5432`, user `postgres.wqpdamlnrwgozfvzjjpo`, pwd `‹secret — gestionnaire de mots de passe›` (le SUPABASE_DB_PASSWORD de powersync/.env = powersync_role, PAS postgres). Connexion directe `db.<ref>.supabase.co` = IPv6 injoignable depuis sandbox. **0019 (RLS slots/lessons) était DÉJÀ live** (schema.sql périmé) → rendu idempotent, non rejoué.

**Sync-rules DÉPLOYÉES sur PowerSync Cloud le 2026-06-28** (validation OK + deploy completed) → les 4 tables descendent offline. PAT = fichier `~/.epilote/powersync.pat` (`jpt_…`, fourni par le user, NE PAS stocker la valeur en mémoire). Cmd : `cd powersync && PS_ADMIN_TOKEN=$(cat ~/.epilote/powersync.pat) npx powersync deploy sync-config --directory=. --instance-id 6a185941234fa2bf51a66757 --sync-config-file-path config/sync-rules.yaml`. ⚠️ Le déploiement prod est bloqué par le classifier sauf autorisation EXPLICITE « déploie » dans le tour. ⚠️ `powersync/sync-config.yaml` (daté 2026-06-10) est PÉRIMÉ — NE PAS éditer ; canonique = `config/sync-rules.yaml`. Gate Vague 0/1 levé : tout est live, analyze projet = 0 issue.

**Vague 1 — paramétrage Salles + Trame : FAITE (code, analyze 0 issue), non testée live (gate déploiement).** Providers `rooms_provider.dart` (catalogue kRoomTypes, CRUD, soft-delete deactivateRoom) + `school_periods_provider.dart` (CRUD + `seedStandardPeriods` trame Congo M1..S3+récré+pause). Écran `edt_settings_screen.dart` (ModuleScaffold slug emploi-du-temps, DefaultTabController 2 onglets) + parts `edt_rooms_tab.dart`/`edt_periods_tab.dart`. Route `Routes.edtParametres = /user/emploi-du-temps/parametres` câblée + bouton « Paramètres » ajouté au header de l'EDT existant.

**Refonte navigation 2026-06-28 (retour user « je suis perdu sur la structure »)** : module EDT = **1 page, 3 onglets** (convention plateforme TabBar, comme admin_groupe) : (1) **Vue d'ensemble** = cockpit santé (cards État/Conflits/Classes couvertes/Créneaux/Salles via `activeTimetableVersionProvider`) + mur mini-grilles par cycle ; (2) **Emplois du temps** = consultation/édition, `_AxisSwitch` Classe/Enseignant/Salle, ScopeDrilldownPanel, KPIs, bandeau conflits, grille, **clic créneau → DRAWER latéral** (`_SlotDrawer`, slide droite, modifier/supprimer/voir classe) ; (3) **Paramètres** = `EdtSettingsView` sous-onglets Salles/Trame/Disponibilités(placeholder Vague 3). Bouton « Paramètres » + route `/parametres` SUPPRIMÉS (Paramètres = onglet). `_edtFocusClassProvider` = pont Vue d'ensemble→onglet Emplois. Fichiers : `emploi_du_temps_screen.dart` (shell tabs), `_overview`/`_schedule`/`_schedule_drawer` (parts), tous ≤500. analyze 0.

**Trame PAR CYCLE : CORRIGÉE 2026-06-28.** `seedStandardPeriods(cycleCode)` choisit le gabarit via `standardTrameFor()` : préscolaire (45min demi-journée), primaire (60min), secondaire collège/lycée/FP (55min). Onglet Trame = `_PeriodsTab` devenu Stateful + `_CycleSelector` (cycles réels via `academicStructureProvider`, pastille count/cycle) ; trame filtrée+seedée+créée par cycle sélectionné. `_PeriodForm` reçoit `cycleCode`. Fichiers : `edt_periods_tab.dart` + `edt_periods_form.dart` (split <500). **Répartition FINALE des 2 onglets (décidée avec user 2026-06-28)** : (A) **Vue d'ensemble** = cockpit santé + **GRAND CALENDRIER MAÎTRE** (`_MasterCalendar`, fichier `emploi_du_temps_calendar.dart`) qui regroupe TOUS les cours de l'école dans 1 grille jour×créneau (cellule = plusieurs cours simultanés empilés en chips, NORMAL pas conflit ; colorés PAR CLASSE) + `_CycleFilter` (chips Tous/cycle) pour la densité ; trame (`schoolPeriodsProvider` course periods) = ossature des lignes ; clic chip → `_edtFocusClassProvider` + bascule onglet Emplois pour éditer. Le **mur de mini-grilles `_OverviewGallery` + `_MiniGridCard/_MiniCell/_MiniEmpty/_CycleSubhead` SUPPRIMÉS** (remplacés par le calendrier maître). (B) **Emplois du temps** = ÉDITEUR PAR ENTITÉ : axe Classe/Enseignant/Salle, drill-down → 1 entité (auto-select 1re classe), grille détaillée `_TimetableGrid` (1 cours/cellule, +case vide=ajouter), drawer détail/édit/suppr, KPIs, conflits, PDF. `_BackToAllChip` supprimé. Conflit-détection partagée (`detectConflicts`). analyze 0, tout ≤500.

⚠️ **(ANCIEN, résolu ci-dessus)** `seedStandardPeriods` installait une trame 100% SECONDAIRE (55min 07:00→16:45) pour TOUS cycles (`cycle_code=NULL`). Réalité base : 5 cycles (prescolaire/primaire/college/lycee/formation_pro), écoles MULTI-CYCLES fréquentes (3/8 ont 3 cycles), primaire/préscolaire ≠ trame lycée + double-flux. `school_periods.cycle_code` EXISTE mais l'onglet Trame l'IGNORE (pas de sélecteur cycle). → refondre onglet Trame PAR CYCLE en lisant cycles réels (`school_cycles`→`education_cycles`). `school_cycles` schema = (school_id, cycle_id→education_cycles.id, group_id). education_cycles codes : prescolaire/primaire/college/lycee/formation_pro.

**Polish Vue d'ensemble 2026-06-28 (analyse sur capture réelle scrot)** : cockpit géant (5 AdminStatCard 176px) → **bande compacte** `_CockpitTile` (icône+valeur+label, Wrap). Calendrier maître amélioré (`_MasterCalendar`/`_CalCell`/`_CalBlock`) : **blocs pleine largeur** (plus de chips 120px), repli **+N autres** (max 4/cellule), **colonne jour courant teintée** (DateTime.now().weekday), cellules vides discrètes. ⚠️ lignes horaires « moches » (chevauchement 07-09/08-09) = DONNÉE (pas de trame installée) → installer trame par cycle nettoie. Lignes hauteur uniforme = choix (style Untis/Pronote vs Google proportionnel). Capture GUI : ⚠️ fenêtres fantômes des runs précédents (X garde la dernière frame) → `xdotool windowkill` + relancer ; scrot via geometry de la fenêtre `epilote`.

**Reste (vagues)** : trame par cycle (correctif) · V2 construction versionnée+conflits durs+vues (refonte écran principal sur version_id/room_id) · V3 dispos enseignant+publication(workflow statut)+PDF · V4 branchements (calendrier fériés, volume horaire vs class_subjects, cahier de textes lié, notifications). Réutiliser : `admin_ui.dart`, `ModuleScaffold`/`runModuleWrite`/`canProvider`, `ScopeDrilldownPanel`, `showPdfPreviewDialog`, `classProgramProvider`, `staffDirectoryProvider`, `activeYearIdProvider`/`yearReadOnlyProvider`. Routes déjà câblées : `/user/emploi-du-temps`, `/user/cahier-textes`.
